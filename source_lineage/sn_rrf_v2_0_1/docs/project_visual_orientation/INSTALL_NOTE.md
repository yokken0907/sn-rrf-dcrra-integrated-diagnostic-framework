# Install note

Copy the entire `docs/project_visual_orientation/` folder into the repository root.

Then open:

```text
docs/project_visual_orientation/index.html
```

This is a standalone browser file.
