# Source lineage note: SN-RRF v2.0.1

This folder preserves the prior SN-RRF v2.0.1 repository package as a parent lineage source for the v3.0.1 complete integrated repository.

Role in v3.0.1:
- establishes the transition from an earlier fixed-exponent A10/TVC-RRF diagnostic representative to a scale-normalized monotone-saturating residual-response family;
- preserves the claim-bounded interpretation that the retained object is diagnostic/hypothesis-generating only;
- supplies manuscript, figures, outputs, scripts, and claim-boundary materials used as source lineage.

This folder is not the active root package for the v3.0.1 release. Active v3.0.1 files are at the repository root and in `paper/`, `outputs/`, `docs/`, `scripts/`, and `tools/`.
